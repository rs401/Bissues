@model Bissues.ViewModels.AdminAreaViewModel
@{
    ViewData["Title"] = "Admin Page";
}

    <div class="col-md-10">
        <h1 class="display-4">Welcome to Admin</h1>
        <p class="mt-3">
            Do the Admin thing.
        </p>
    </div>
    <hr/>

    <div class="container px-4">
        @foreach (var item in Model.Bissues)
        {
            <div class = "card mb-2">
                <a asp-controller="Admin" asp-action="BissueDetails" asp-route-id="@item.Id" class = "btn text-left text-decoration-none">
                    <div class="card-body">
                        <p class="card-title"><b>@item.Title</b> 
                            @if(item.IsOpen)
                            {
                                <span class="text-success">Open</span>
                            }
                            else
                            {
                                <span class="text-danger">Closed</span>
                            }
                        </p>
                        <p>
                            @if(item.Label == BissueLabel.Issue)
                            {
                                <span class="text-success">Issue</span>
                            }
                            else
                            {
                                <span class="text-danger">Bug</span>
                            }
                        </p>
                        <p class="card-text">@item.Description</p>
                        <footer>Project: @item.Project.Name</footer>
                    </div>
                        
                </a>
            </div>
        }
    </div>


    @* <h4>Recent Bissues</h4>
    @foreach (var bissue in Model.Bissues)
    {
        <div class = "card mb-2">
            <a asp-controller="Bissues" asp-action="Details" asp-route-id="@bissue.Id" class = "btn text-left text-decoration-none">
                <div class="card-body">
                    <p class="card-title"><b>@bissue.Title</b> 
                        @if(bissue.IsOpen)
                        {
                            <span class="text-success">Open</span>
                        }
                        else
                        {
                            <span class="text-danger">Closed</span>
                        }
                    </p>
                    <p class="card-text">@bissue.Description</p>
                    <footer>Project: @bissue.Project.Name</footer>
                </div>
            </a>
        </div>
    } *@


